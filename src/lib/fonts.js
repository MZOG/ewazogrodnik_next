import { Inter, DM_Serif_Display } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});

export const serif_display = DM_Serif_Display({
  subsets: ['latin'],
  display: 'swap',
  weight: ["400"]
});